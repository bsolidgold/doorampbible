# Doo League — dooleague.com

## Status: Placeholder

This subfolder will become the site for **dooleague.com**.

## Deployment

Currently part of the main Dooramp Bible Vercel deployment. When ready to go live on its own domain, this folder can be:
1. Configured as a separate Vercel project pointing to this subfolder, or
2. Split into its own repository

## Getting Started

Edit `index.html` to build out the site. Shares `../style.css` from the root for now — add a local `style.css` if this site needs its own look.
